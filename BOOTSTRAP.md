# Moataz AI repository bootstrap

This temporary file initializes the empty repository. It will be removed automatically when the full Moataz AI source tree is materialized from the pinned RikkaHub Agent upstream commit plus the reviewed Moataz AI patch.
